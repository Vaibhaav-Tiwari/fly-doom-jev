# Jev (Moonshot/Kimi OpenAI-compatible) API — server-side only. NEVER expose to the browser.
# Leave JEV_API_KEY empty to run with the deterministic mock Jev (default).
JEV_API_KEY=
JEV_BASE_URL=https://api.moonshot.ai/v1
JEV_MODEL=moonshot-v1-8k
JEV_TIMEOUT_SECONDS=10
JEV_MAX_RETRIES=2
