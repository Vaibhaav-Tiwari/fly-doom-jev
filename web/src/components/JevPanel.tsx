import type {ReplayStep} from '../lib/types';import {pct} from '../lib/recording';
export default function JevPanel({step,history,onSeek}:{step:ReplayStep|null;history:ReplayStep[];onSeek:(t:number)=>void}){const j=step?.jev;const questions=j?.questions?.map(q=>[q.question,q.probability??q.score??0] as const)??Object.entries(j?.probabilities??{});const confidence=j?.confidence??Math.max(0,...questions.map(([,v])=>v));return <div className="jev-body"><div className="jev-meta"><span>{j?.model??'No model metadata'}</span><span className={j?.is_mock?'warning':''}>{j?.is_mock?'MOCK JEV':'RECORDED JEV'}</span><b>{j?.latency_ms?.toFixed(1)??'—'} ms</b></div><div className="question-list">{questions.length?questions.map(([q,p])=><div className="question" key={q}><div><span>{q.replaceAll('_',' ')}</span><b>{pct(p)}</b></div><div className="bar"><i style={{width:pct(Math.min(1,p))}}/></div></div>):<div className="empty">No per-question probabilities recorded.</div>}</div><div className="confidence"><span>Decision confidence</span><strong>{pct(confidence)}</strong></div><div className="decision-strip">{history.slice(-8).map((s,i)=><button key={`${s.t_ms}-${i}`} onClick={()=>onSeek(s.t_ms)} title={`${s.t_ms.toFixed(0)} ms`}>{s.motor?.selected??s.motor?.selected_action??'—'}</button>)}</div></div>}
